{
  "name": "dev-holic-monorepo",
  "private": true,
  "scripts": {
    "lint": "turbo run lint",
    "copy-assets": "node packages/posts/scripts/copy-assets.js",
    "build": "pnpm run copy-assets && turbo run build",
    "dev": "pnpm run copy-assets && turbo run dev"
  },
  "devDependencies": {
    "turbo": "latest",
    "prettier": "^3.3.3"
  },
  "packageManager": "pnpm@10.26.1",
  "engines": {
    "node": ">=18"
  }
}
